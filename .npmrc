# This root package.json only delegates scripts into frontend/ (pnpm -C frontend …).
# It owns no dependencies, so pnpm's pre-run "are deps in sync with the lockfile?"
# check has nothing valid to check against and would otherwise auto-run `install`.
# Turn it off here; the real install happens in frontend/ with its own lockfile.
verify-deps-before-run=false
