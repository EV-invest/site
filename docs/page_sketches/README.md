source of truth for **intent** of site design

figma pages can further be generated from it, with actual design of constituent components; but this must remain the source of truth for intent.
No figma/code changes can go to corresponding master branch until they match sketches.
If you deviated from it while designing code or figma expressions, - keep it on a dev branch, and wait for a PR syncing the sketch to land, before pushing it further.
