## Rust site (`ev_site`)
```sh
dx serve -p ev_site
```

## Manus JS sites
Vite + pnpm (pinned pnpm@10.4.1, provisioned by corepack in the dev shell). `pnpm dev` serves on `:3000` with `--host`.
```sh
cd manus_site_vietnam_colors && pnpm install && pnpm dev   # or: manus_site_good_team_page
```
